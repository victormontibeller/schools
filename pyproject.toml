[tool.black]
line-length = 100
target-version = ["py313"]
include = '\.pyi?$'
exclude = '''
/(
    migrations
  | \.git
  | \.venv
  | build
)/
'''

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4", "T20", "DJ", "S"]
ignore = [
    "DJ001",   # Avoid using null=True on string-based fields — we handle it explicitly
    "A003",    # Class attribute shadowing builtin (id, type etc.)
    "DJ012",   # Mis-fires on the `all_objects = models.Manager()` next to a custom default manager;
               # reordering would change Django's default-manager semantics.
]
exclude = ["migrations", ".git", ".venv", "build"]

[tool.ruff.lint.per-file-ignores]
"*/tests/*" = ["N802", "S101", "S105", "S106", "ANN"]
"*/conftest.py" = ["N802", "S101", "S105", "S106", "ANN"]
"conftest.py" = ["N802", "S101", "S105", "S106", "ANN"]
"*/migrations/*" = ["ALL"]
"scripts/*" = ["T20", "E402", "E501"]

[tool.coverage.run]
source = ["base", "tenancy", "core", "accounts", "locations", "audit", "teachers", "students", "guardians", "classes", "rooms", "agenda", "activities", "academic_calendar", "attendance", "student_diary", "enrollments", "notifications", "dashboard", "addresses", "financeiro"]
omit = ["*/migrations/*", "*/tests/*", "*/conftest.py", "manage.py"]

[tool.coverage.report]
skip_empty = true
fail_under = 85
